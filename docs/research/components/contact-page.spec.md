# ContactPage specification

- Target: `src/components/pages/ContactPage.tsx` and `src/app/contact/page.tsx`
- References: `codezela-contact-desktop-top.png`, `codezela-contact-y850.png`, `codezela-contact-y1550.png`, `codezela-contact-y2250.png`, mobile top reference.
- Hero copy: `Getting in touch with our departments have never been easier`; 52px/1.04 purple. Right shows the local pale headset asset plus purple `Schedule a Call` and `Request a Proposal` pills.
- H1 `Contact Us` with the exact live intro below.
- Support Lines: centered purple phone heading and three desktop columns separated by thin purple rules. General phone, WhatsApp, and Sales Agent must use working `tel:`/WhatsApp links.
- General Inquiries: centered email heading and `mailto:` link.
- Locations: four equal desktop columns with exact labels and addresses from the live contact extraction. Correct the obvious live heading typo to `Headquarters`, as the user explicitly requested spelling fixes.
- Socials: LinkedIn, Facebook, Instagram, TikTok, X, Threads, YouTube, Behance, WhatsApp with their correct brand icons and live destinations.
- Mobile stacks each contact/location item with dividers; all controls keep at least 44px touch targets.
- Request Proposal opens the existing `ProposalDialog`; Schedule opens Calendly in a new tab.
